class Car {
    model: string;
    year: number;
    price: string;

    constructor(model: string, year: number, price: string)
    {this.model = model; this.year = year; this.price =price}
}



const car1 = new Car('Tesla', 2000, '200000');

class person {
    name: string;
    age: number;

    constructor(name:string, age:number)
    {this.name = name; this.age = age}
    
    }

    const person2 = new person('Muhamada', 25)


    // class cheff extends person {
    //     constructor(name:string, age:number, occupation: string)
        
    //     {
    //     super(name , age); 
    //     this.occupation = occupation;
    //     }

    // }


    